# 🔧 Проблема: Запросы идут не на бэкенд

## Проблема

В логах видно что запросы идут на:
```
/xalava-mp3-production.up.railway.app/tracks
```

Вместо правильного:
```
https://your-backend.railway.app/api/tracks
```

Это значит что `VITE_API_URL` не установлен или неправильный.

## Решение

### Шаг 1: Найдите URL вашего бэкенда

1. В Railway откройте сервис **Backend**
2. Перейдите в **Settings**
3. Найдите секцию **"Domains"** или **"Generate Domain"**
4. Скопируйте URL (например: `https://xalava-backend.railway.app`)

### Шаг 2: Установите переменную для фронтенда

1. В Railway откройте сервис **Frontend**
2. Перейдите в **Variables**
3. Добавьте или обновите переменную:

```
VITE_API_URL=https://your-backend-url.railway.app/api
```

**ВАЖНО:** 
- Замените `your-backend-url.railway.app` на реальный URL из шага 1
- URL должен заканчиваться на `/api`

### Шаг 3: Пересоберите фронтенд

После добавления переменной Railway автоматически пересоберёт проект. Подождите 1-2 минуты.

### Шаг 4: Проверка

1. Откройте сайт
2. Откройте консоль браузера (F12)
3. Перейдите на вкладку **Network**
4. Обновите страницу
5. Проверьте что запросы идут на правильный URL бэкенда

---

## Если фронтенд и бэкенд на одном сервисе

Если вы случайно разместили фронтенд и бэкенд на одном сервисе:

1. **Создайте отдельный сервис для фронтенда:**
   - В Railway нажмите "New Service"
   - Выберите "Empty Service"
   - Подключите тот же репозиторий
   - Root Directory: `frontend`
   - Build Command: `npm install && npm run build`
   - Start Command: `npx serve -s dist -l 3000`

2. **Установите переменную `VITE_API_URL`** с URL бэкенда

---

## Проверка что всё работает

После исправления в логах должны быть запросы:
- ✅ `GET /api/tracks` (200 OK)
- ✅ `GET /api/podcasts` (200 OK)
- ✅ `GET /api/recommendations` (200 OK)

А не:
- ❌ `GET /xalava-mp3-production.up.railway.app/tracks` (404)

