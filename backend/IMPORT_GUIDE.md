# 📥 Руководство по автоматическому импорту подкастов

## Варианты импорта данных

### 1. Быстрый старт с готовыми данными (рекомендуется)

Просто запустите:
```bash
cd backend
npm run seed
```

Это создаст:
- 5 подкастов
- 50 эпизодов (по 10 на каждый подкаст)
- 20 треков

**Не нужно ничего настраивать!**

---

### 2. Импорт из RSS фидов (для реальных подкастов)

Если у вас есть RSS фиды подкастов, вы можете импортировать их автоматически:

1. **Откройте файл** `backend/src/scripts/import-podcasts.ts`

2. **Найдите строку:**
   ```typescript
   const PODCAST_RSS_FEEDS = [
     'https://feeds.simplecast.com/54nAGcIl',
     // Добавьте сюда свои RSS фиды
   ]
   ```

3. **Добавьте свои RSS фиды:**
   ```typescript
   const PODCAST_RSS_FEEDS = [
     'https://example.com/podcast/rss',
     'https://another-podcast.com/feed.xml',
     // и т.д.
   ]
   ```

4. **Запустите импорт:**
   ```bash
   cd backend
   npm run import
   ```

Скрипт автоматически:
- Скачает информацию о подкасте из RSS
- Импортирует все эпизоды
- Сохранит в вашу базу данных

---

### 3. Где найти RSS фиды подкастов?

**Популярные источники:**
- **Apple Podcasts**: Откройте страницу подкаста → Скопируйте RSS ссылку
- **Spotify**: Многие подкасты имеют RSS фиды
- **Google Podcasts**: RSS ссылки доступны на страницах подкастов
- **Сайт подкаста**: Обычно есть ссылка "RSS" или "Subscribe"

**Примеры RSS фидов:**
```
https://feeds.npr.org/510289/podcast.xml
https://rss.cnn.com/rss/edition.rss
https://feeds.simplecast.com/54nAGcIl
```

---

### 4. Добавление своих подкастов вручную

Если вы хотите добавить подкасты вручную, отредактируйте массив `POPULAR_PODCASTS` в файле `backend/src/scripts/import-podcasts.ts`:

```typescript
const POPULAR_PODCASTS = [
  {
    title: 'Название вашего подкаста',
    description: 'Описание подкаста',
    coverUrl: 'https://ссылка-на-обложку.jpg',
    author: 'Автор подкаста',
    genres: ['Жанр 1', 'Жанр 2'],
    rssUrl: null, // или RSS ссылка если есть
  },
  // Добавьте больше подкастов...
]
```

Затем запустите:
```bash
npm run import
```

---

### 5. Комбинированный подход

Вы можете использовать оба метода:

1. Сначала создайте базовые данные:
   ```bash
   npm run seed
   ```

2. Затем добавьте подкасты из RSS:
   ```bash
   npm run import
   ```

Все данные будут объединены в одной базе!

---

## Часто задаваемые вопросы

**Q: Можно ли использовать оба скрипта?**
A: Да! `seed` создаёт базовые данные, `import` добавляет больше. Они не конфликтуют.

**Q: Что если RSS фид не работает?**
A: Скрипт пропустит его и продолжит с остальными. Проверьте ссылку в браузере.

**Q: Как добавить больше треков?**
A: Отредактируйте функцию `generateTracks()` в `seed.ts` - увеличьте число в цикле `for (let i = 0; i < 20; i++)`

**Q: Можно ли импортировать из Spotify/Apple Music?**
A: Прямой импорт из этих сервисов требует API ключи. Но если у подкаста есть публичный RSS фид, используйте его.

---

## Примеры популярных RSS фидов

Добавьте эти в `PODCAST_RSS_FEEDS` для тестирования:

```typescript
const PODCAST_RSS_FEEDS = [
  'https://feeds.npr.org/510289/podcast.xml', // NPR
  'https://rss.cnn.com/rss/edition.rss',      // CNN
  // Найдите RSS фиды интересующих вас подкастов
]
```

**Важно:** Убедитесь, что RSS фид содержит аудио файлы (enclosure с type="audio/mpeg")

