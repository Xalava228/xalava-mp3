# Online Radio & Podcast Web App

Полнофункциональный веб-сайт онлайн-радио и подкастов с каталогом выпусков, встроенным аудиоплеером, персональными рекомендациями и современной тёмной UI-темой.

## Технологии

### Frontend
- React + TypeScript
- TailwindCSS
- React Router
- Zustand (state management)
- React Query (data fetching)

### Backend
- Node.js + Express
- JSON файловое хранилище (не нужен MongoDB!)
- JWT Authentication

## Установка

### Предварительные требования

- Node.js 18+ 
- MongoDB (локально или MongoDB Atlas)
- npm или yarn

### Шаги установки

1. Установите зависимости для всех частей проекта:
```bash
npm run install:all
```

2. Настройте переменные окружения:

**Backend:**
```bash
cd backend
# Скопируйте файл .env.example в .env
# Windows:
copy .env.example .env
# Linux/Mac:
cp .env.example .env

# Затем откройте файл .env в текстовом редакторе
# и замените JWT_SECRET на любую случайную строку, например:
# JWT_SECRET=мой-секретный-ключ-12345
```

**Frontend (опционально, обычно не требуется):**
```bash
cd frontend
# Файл .env.example уже содержит правильные настройки
# Если нужно изменить URL API, скопируйте и отредактируйте:
cp .env.example .env
```

3. Заполните базу данных тестовыми данными:
```bash
cd backend
npm run seed
```
Это создаст тестовые подкасты, эпизоды и треки в папке `backend/data/`

5. Запустите проект:

**Терминал 1 - Backend (оставьте открытым):**
```bash
cd backend
npm run dev
```
Вы должны увидеть: `Server running on port 5000`

**Терминал 2 - Frontend (новое окно терминала):**
```bash
cd frontend
npm run dev
```
Вы должны увидеть: `Local: http://localhost:3000/`

6. Откройте браузер и перейдите по адресу:
```
http://localhost:3000
```

**Готово!** Сайт должен открыться. 🎉

📖 **Подробная инструкция:** см. файл `ЗАПУСК.md`

## Структура проекта

```
├── frontend/          # React приложение
├── backend/           # Express API
└── README.md
```

## Функциональность

- 🎵 Аудиоплеер с управлением воспроизведением
- 📻 Каталог подкастов и эпизодов
- 🔍 Поиск по трекам, подкастам и авторам
- 👤 Система авторизации
- 📚 Персональная библиотека
- 🎯 Рекомендации на основе истории прослушиваний
- 📱 Адаптивный дизайн
- ⬆️ Загрузка треков пользователями (требует авторизации)

## Хостинг

**Да, другие пользователи смогут загружать треки!**

После регистрации любой пользователь может:
- Загружать свои треки через веб-интерфейс
- Файлы автоматически сохраняются на сервере
- Все пользователи видят загруженные треки

📖 **Подробное руководство по хостингу:** см. `HOSTING_GUIDE.md`

