# ✅ Чек-лист для продакшена

## 🔧 Настройка переменных окружения

### Frontend (.env)
```env
VITE_API_URL=https://your-domain.com/api
```

### Backend (.env)
```env
PORT=5000
JWT_SECRET=очень-длинный-случайный-секретный-ключ-минимум-32-символа
FRONTEND_URL=https://your-domain.com
BASE_URL=https://your-domain.com
NODE_ENV=production
```

## 🚀 Шаги для деплоя

1. **Создайте .env файлы** на основе .env.example
2. **Измените JWT_SECRET** на случайную строку (минимум 32 символа)
3. **Укажите правильные URL** для FRONTEND_URL и BASE_URL
4. **Соберите frontend**: `cd frontend && npm run build`
5. **Соберите backend**: `cd backend && npm run build`
6. **Запустите backend**: `cd backend && npm start`

## ⚠️ Важные моменты

- ✅ Все хардкоды localhost заменены на переменные окружения
- ✅ CORS настроен для продакшена
- ✅ URL файлов генерируются динамически
- ⚠️ Убедитесь, что папка `backend/uploads/` доступна для записи
- ⚠️ Настройте резервное копирование `backend/data/` и `backend/uploads/`
- ⚠️ Используйте HTTPS в продакшене
- ⚠️ Настройте rate limiting для защиты от злоупотреблений

## 📦 Рекомендации для хостинга

- **VPS**: DigitalOcean, Linode, Hetzner
- **PaaS**: Railway, Render, Heroku
- **Файлы**: AWS S3, Cloudinary (для больших проектов)

## 🔒 Безопасность

- [ ] Измените JWT_SECRET
- [ ] Настройте HTTPS
- [ ] Добавьте rate limiting
- [ ] Настройте firewall
- [ ] Регулярно обновляйте зависимости

