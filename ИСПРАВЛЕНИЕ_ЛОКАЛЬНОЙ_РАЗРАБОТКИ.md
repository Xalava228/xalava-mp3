# 🔧 Исправление ошибок CORS при локальной разработке

## Проблема

В консоли браузера видны ошибки:
```
Access to XMLHttpRequest at 'https://your-backend-domain.com/api/...' 
from origin 'http://localhost:3000' has been blocked by CORS policy
```

**Причина:** `VITE_API_URL` не установлен или установлен неправильно.

## Решение

### Шаг 1: Создайте файл `.env` в папке `frontend/`

Создайте файл `frontend/.env` со следующим содержимым:

```env
VITE_API_URL=http://localhost:5000/api
```

### Шаг 2: Перезапустите фронтенд

После создания `.env` файла:

1. Остановите фронтенд (Ctrl+C)
2. Запустите снова:
   ```bash
   cd frontend
   npm run dev
   ```

**Важно:** Vite загружает переменные окружения только при запуске. После изменения `.env` нужно перезапустить!

### Шаг 3: Проверьте что бэкенд запущен

Убедитесь, что бэкенд запущен на порту 5000:

```bash
cd backend
npm run dev
```

Должно быть:
```
🚀 Server running on port 5000
```

### Шаг 4: Проверьте CORS в бэкенде

В `backend/src/index.ts` должно быть:

```typescript
const frontendUrl = process.env.FRONTEND_URL
  ? process.env.FRONTEND_URL.replace(/\/$/, '')
  : (NODE_ENV === 'production' ? '*' : 'http://localhost:3000')

const corsOptions = {
  origin: frontendUrl === '*' ? '*' : [frontendUrl, `${frontendUrl}/`],
  credentials: true,
  optionsSuccessStatus: 200
}
```

Для локальной разработки это должно разрешать запросы с `http://localhost:3000`.

## Проверка

1. Откройте консоль браузера (F12)
2. Обновите страницу
3. Проверьте что запросы идут на `http://localhost:5000/api/...`
4. Ошибки CORS должны исчезнуть

## Если ошибка остаётся

1. **Проверьте что `.env` файл в правильной папке:**
   - Должен быть в `frontend/.env`
   - НЕ в `frontend/src/.env`
   - НЕ в корне проекта

2. **Проверьте содержимое `.env`:**
   ```bash
   cd frontend
   type .env
   ```
   Должно быть: `VITE_API_URL=http://localhost:5000/api`

3. **Очистите кэш Vite:**
   ```bash
   cd frontend
   rm -rf node_modules/.vite
   npm run dev
   ```

4. **Проверьте что бэкенд запущен:**
   - Откройте `http://localhost:5000/api/health` в браузере
   - Должен вернуть: `{"status":"ok"}`

## Альтернатива: Использовать прокси Vite

Если проблемы остаются, можно использовать встроенный прокси Vite (уже настроен в `vite.config.ts`):

Вместо `http://localhost:5000/api` используйте относительный путь `/api`:

```typescript
// В frontend/src/api/client.ts
const API_URL = import.meta.env.VITE_API_URL || '/api'
```

Но лучше использовать `.env` файл для явной настройки.

