# 🔧 Исправление ошибки деплоя на Railway

## Проблема

Railway не может определить тип проекта и выдает ошибку "Error creating build plan with Railpack".

## Решение

### Вариант 1: Правильная настройка в Railway (рекомендуется)

1. В Railway откройте ваш сервис
2. Перейдите в **Settings**
3. Установите следующие параметры:

**Root Directory:**
```
backend
```

**Build Command:**
```
npm install && npm run build
```

**Start Command:**
```
npm start
```

4. Перейдите в **Variables** и добавьте:
```
NODE_ENV=production
JWT_SECRET=<ваш-сгенерированный-ключ>
FRONTEND_URL=https://your-frontend.railway.app
BASE_URL=https://your-backend.railway.app
```

5. Нажмите **Redeploy** (или Railway перезапустит автоматически)

### Вариант 2: Использование railway.json

Я создал файл `backend/railway.json` который Railway должен автоматически подхватить.

Если ошибка остаётся:

1. Убедитесь что в Settings → Root Directory указано: `backend`
2. Убедитесь что Start Command: `npm start`
3. Попробуйте удалить сервис и создать заново

### Вариант 3: Использование Dockerfile (если ничего не помогает)

Создайте файл `backend/Dockerfile`:

```dockerfile
FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .
RUN npm run build

EXPOSE 5000

CMD ["npm", "start"]
```

Затем в Railway:
- Settings → Dockerfile Path: `backend/Dockerfile`

---

## Проверка

После исправления:
1. Railway должен успешно собрать проект
2. В логах вы увидите "Server running on port 5000"
3. Сервис станет доступен по URL

## Если всё ещё не работает

1. Проверьте логи в Railway (кнопка "View logs")
2. Убедитесь что все файлы загружены в GitHub
3. Проверьте что `backend/package.json` существует
4. Попробуйте создать новый сервис с нуля

