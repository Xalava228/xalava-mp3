# 🔧 Исправление CORS ошибки

## Проблема

В консоли браузера видна ошибка:
```
Access-Control-Allow-Origin header has a value 'https://carefree-flexibility-production-d63e.up.railway.app/' 
that is not equal to the supplied origin.
```

**Причина:** `FRONTEND_URL` в бэкенде указан со слешем в конце (`/`), а браузер отправляет запрос без слеша.

## Решение

### Вариант 1: Исправить FRONTEND_URL (рекомендуется)

1. В Railway откройте **Backend** (xalava-mp3) → **Variables**
2. Найдите `FRONTEND_URL`
3. Измените на:
   ```
   https://carefree-flexibility-production-d63e.up.railway.app
   ```
   **Убедитесь что НЕТ слеша `/` в конце!**

4. Сохраните и подождите 1-2 минуты

### Вариант 2: Использовать * (для теста)

1. В Railway откройте **Backend** → **Variables**
2. Измените `FRONTEND_URL` на:
   ```
   *
   ```
3. Сохраните

**⚠️ Внимание:** `*` разрешает запросы с любого домена. Для продакшена лучше использовать конкретный URL.

---

## Что я исправил в коде

Я обновил `backend/src/index.ts` чтобы автоматически:
- Убирать слеш в конце `FRONTEND_URL`
- Разрешать запросы как с слешем, так и без

**Но вам всё равно нужно:**
1. Обновить код на сервере (сделать commit и push)
2. Или исправить `FRONTEND_URL` в Railway (убрать `/` в конце)

---

## После исправления

1. Обновите страницу (Ctrl+F5)
2. Попробуйте зарегистрироваться
3. Ошибки CORS должны исчезнуть

