# 📖 Подробная инструкция по запуску

## Пункт 2: Создание файла настроек

### Что нужно сделать:

1. **Откройте папку `backend`** в вашем проекте

2. **Создайте файл с именем `.env`** (точка в начале важна!)

   **Способ 1 - через проводник Windows:**
   - Откройте папку `backend`
   - Правой кнопкой → Создать → Текстовый документ
   - Переименуйте в `.env` (уберите расширение .txt)

   **Способ 2 - через терминал:**
   ```bash
   cd backend
   copy .env.example .env
   ```
   (Если файла .env.example нет, создайте файл .env вручную)

3. **Откройте файл `.env` в блокноте** или любом текстовом редакторе

4. **Вставьте туда этот текст:**
   ```
   PORT=5000
   JWT_SECRET=мой-секретный-ключ-12345
   NODE_ENV=development
   ```

5. **Замените `мой-секретный-ключ-12345`** на любую строку (например: `secret123`, `my-key-2024`, `abc123xyz`)

   **Важно:** Это просто пароль для безопасности. Можете написать что угодно!

6. **Сохраните файл**

**Готово!** Файл `.env` создан и настроен.

---

## Пункт 3: Заполнение тестовыми данными

### Что нужно сделать:

1. **Откройте терминал** (PowerShell, CMD или Git Bash)

2. **Перейдите в папку backend:**
   ```bash
   cd backend
   ```
   (Или если вы уже в корне проекта: `cd C:\Users\bogda\OneDrive\Desktop\kyrsach\backend`)

3. **Запустите команду:**
   ```bash
   npm run seed
   ```

4. **Подождите несколько секунд** - вы увидите сообщения:
   ```
   Starting seed...
   Created 5 podcasts
   Created 25 episodes
   Created 3 tracks
   Seed completed successfully!
   ```

5. **Проверьте:** В папке `backend` должна появиться новая папка `data` с файлами:
   - `podcasts.json`
   - `episodes.json`
   - `tracks.json`
   - `users.json` (пока пустой)
   - `listeningHistory.json` (пока пустой)

**Готово!** Тестовые данные созданы.

---

## Визуальная схема:

```
kyrsach/
├── backend/
│   ├── .env          ← ВЫ СОЗДАЁТЕ ЭТОТ ФАЙЛ (пункт 2)
│   ├── data/         ← ЭТА ПАПКА СОЗДАСТСЯ АВТОМАТИЧЕСКИ (пункт 3)
│   │   ├── podcasts.json
│   │   ├── episodes.json
│   │   └── tracks.json
│   └── ...
└── frontend/
```

---

## Если что-то не получается:

### Ошибка "файл .env не найден"
- Убедитесь, что файл называется именно `.env` (с точкой в начале)
- Убедитесь, что файл находится в папке `backend`

### Ошибка при запуске seed
- Убедитесь, что выполнили `npm install` в папке backend
- Убедитесь, что вы находитесь в папке `backend` когда запускаете команду

### Не понимаю, что делать дальше
- Откройте файл `QUICK_START.md` - там пошаговая инструкция с картинками
- Или просто следуйте пунктам 4 и 5 из основного README.md

