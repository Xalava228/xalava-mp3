# 🚀 Простой деплой за 10 минут

## Самый простой способ - Railway (всё автоматически!)

### Шаг 1: Регистрация (2 минуты)

1. Перейдите на https://railway.app
2. Нажмите "Start a New Project"
3. Войдите через GitHub (если нет аккаунта - создайте бесплатно)

### Шаг 2: Загрузите проект в GitHub (3 минуты)

1. Откройте https://github.com/new
2. Назовите репозиторий: `xalava-mp3`
3. НЕ ставьте галочки (README, .gitignore)
4. Нажмите "Create repository"

**В терминале выполните:**
```bash
cd C:\Users\bogda\OneDrive\Desktop\kyrsach
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ВАШ_ЛОГИН/xalava-mp3.git
git push -u origin main
```
(Замените `ВАШ_ЛОГИН` на ваш GitHub логин)

### Шаг 3: Деплой Backend (2 минуты)

1. В Railway нажмите "New Project"
2. Выберите "Deploy from GitHub repo"
3. Выберите ваш репозиторий `xalava-mp3`
4. Нажмите на созданный сервис
5. Перейдите в **Settings** (вкладка сверху) и установите:

   **⚠️ ВАЖНО: Root Directory**
   ```
   backend
   ```
   (Это самое важное! Railway должен знать где искать package.json)

   **Build Command:**
   ```
   npm install && npm run build
   ```

   **Start Command:**
   ```
   npm start
   ```

6. Перейдите в **Variables** (вкладка сверху) и добавьте:

```
NODE_ENV=production
JWT_SECRET=ваш-секретный-ключ-сюда
```

**Сгенерируйте JWT_SECRET:**
```bash
node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
```

9. Railway автоматически соберет и запустит!

10. В "Settings" нажмите "Generate Domain" - скопируйте URL (например: `https://xalava-backend.railway.app`)

### Шаг 4: Деплой Frontend (2 минуты)

1. В Railway нажмите "New Service" → "Empty Service"
2. Нажмите на сервис → "Settings"
3. В "Source" выберите ваш репозиторий
4. В "Root Directory" укажите: `frontend`
5. В "Build Command" укажите: `npm install && npm run build`
6. В "Start Command" укажите: `npx serve -s dist -l 3000`
7. **ВАЖНО:** Перейдите в **Variables** и добавьте:

```
VITE_API_URL=https://your-backend-url.railway.app/api
```

**⚠️ КРИТИЧЕСКИ ВАЖНО:**
- Замените `your-backend-url.railway.app` на **реальный URL бэкенда** из шага 3.10
- URL должен заканчиваться на `/api`
- Без этой переменной фронтенд не сможет подключиться к бэкенду!

8. В "Settings" нажмите "Generate Domain" - скопируйте URL фронтенда

### Шаг 5: Финальная настройка (1 минута)

1. Вернитесь в **Backend** → **Variables**
2. Добавьте/обновите:

```
FRONTEND_URL=https://your-frontend-url.railway.app
BASE_URL=https://your-backend-url.railway.app
```

(Замените на реальные URL из шагов 3.10 и 4.8)

3. **Проверьте что в Frontend → Variables установлен правильный `VITE_API_URL`**

4. Railway автоматически пересоберёт оба сервиса

3. Railway автоматически перезапустит сервисы

### Готово! 🎉

Откройте URL фронтенда в браузере - ваш сайт работает!

---

## Еще проще: Render.com (альтернатива)

### Backend:

1. https://render.com → "New" → "Web Service"
2. Подключите GitHub репозиторий
3. Настройки:
   - Root Directory: `backend`
   - Build: `npm install && npm run build`
   - Start: `npm start`
4. Добавьте переменные:
   - `NODE_ENV=production`
   - `JWT_SECRET=<ваш-ключ>`
5. Нажмите "Create"

### Frontend:

1. "New" → "Static Site"
2. Подключите репозиторий
3. Настройки:
   - Root Directory: `frontend`
   - Build: `npm install && npm run build`
   - Publish: `dist`
4. Добавьте переменную:
   - `VITE_API_URL=https://your-backend.onrender.com/api`
5. Нажмите "Create"

---

## Что делать если что-то не работает?

1. **Проверьте логи** - в Railway/Render есть вкладка "Logs"
2. **Проверьте переменные** - все ли установлены правильно
3. **Проверьте URL** - правильные ли адреса в переменных

---

## Важно помнить:

- ✅ JWT_SECRET должен быть уникальным (сгенерируйте свой!)
- ✅ URL должны быть реальными (из Railway/Render)
- ✅ После изменения переменных сервисы перезапускаются автоматически

**Всё! Это самый простой способ. Никаких серверов, никаких настроек - всё автоматически!** 🚀

