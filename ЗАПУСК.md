# 🚀 Как запустить сайт - пошаговая инструкция

## Шаг 1: Установка зависимостей (если ещё не делали)

Откройте терминал в корневой папке проекта (`kyrsach`) и выполните:

```bash
npm run install:all
```

Подождите, пока установятся все библиотеки (может занять 1-2 минуты).

---

## Шаг 2: Создание файла настроек (если ещё не делали)

1. Откройте папку `backend`
2. Создайте файл `.env` (с точкой в начале!)
3. Вставьте в него:
```
PORT=5000
JWT_SECRET=мой-секретный-ключ-12345
NODE_ENV=development
```
4. Сохраните файл

---

## Шаг 3: Заполнение тестовыми данными (если ещё не делали)

Откройте терминал и выполните:

```bash
cd backend
npm run seed
```

Вы увидите сообщения о создании данных. Подождите несколько секунд.

---

## Шаг 4: Запуск Backend (Терминал 1)

1. Откройте **первый терминал** (PowerShell, CMD или Git Bash)
2. Выполните:
```bash
cd backend
npm run dev
```

Вы должны увидеть:
```
Server running on port 5000
Data stored in: .../backend/data
```

**Оставьте этот терминал открытым!** Backend должен работать постоянно.

---

## Шаг 5: Запуск Frontend (Терминал 2)

1. Откройте **второй терминал** (новое окно терминала)
2. Выполните:
```bash
cd frontend
npm run dev
```

Вы должны увидеть:
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:3000/
```

---

## Шаг 6: Открытие сайта в браузере

Откройте браузер (Chrome, Firefox, Edge) и перейдите по адресу:

```
http://localhost:3000
```

**Готово!** Сайт должен открыться. 🎉

---

## Визуальная схема:

```
Терминал 1 (Backend):
┌─────────────────────────┐
│ cd backend             │
│ npm run dev            │
│                        │
│ Server running on      │
│ port 5000              │
└─────────────────────────┘
         ↓
    Backend работает
    на порту 5000

Терминал 2 (Frontend):
┌─────────────────────────┐
│ cd frontend            │
│ npm run dev            │
│                        │
│ Local:                 │
│ http://localhost:3000/ │
└─────────────────────────┘
         ↓
    Откройте в браузере
    http://localhost:3000
```

---

## Что делать дальше?

1. **Зарегистрируйтесь** - нажмите "Зарегистрироваться" и создайте аккаунт
2. **Изучите интерфейс** - посмотрите подкасты, эпизоды, треки
3. **Попробуйте поиск** - введите что-то в поиск
4. **Воспроизведите трек** - нажмите кнопку Play на любом треке/эпизоде

---

## Если что-то не работает:

### Ошибка "Cannot find module"
```bash
# Установите зависимости заново
npm run install:all
```

### Backend не запускается
- Проверьте, что файл `backend/.env` существует
- Проверьте, что в `.env` указан `JWT_SECRET`

### Frontend не открывается
- Убедитесь, что backend запущен (Терминал 1)
- Проверьте, что порт 3000 свободен

### Сайт открывается, но ничего не загружается
- Проверьте, что backend работает (должен быть запущен в Терминале 1)
- Откройте консоль браузера (F12) и посмотрите на ошибки

### Порт уже занят
- Закройте другие программы, использующие порты 3000 или 5000
- Или измените порты в настройках

---

## Быстрая команда (если всё уже настроено):

Если вы уже делали шаги 1-3, просто запустите:

**Терминал 1:**
```bash
cd backend && npm run dev
```

**Терминал 2:**
```bash
cd frontend && npm run dev
```

И откройте http://localhost:3000 в браузере!

