# ⚡ Быстрое исправление CORS ошибок

## Проблема найдена!

В файле `frontend/.env` был неправильный URL:
```
VITE_API_URL=https://your-backend-domain.com/api
```

## Решение

Я исправил файл `.env`. Теперь нужно:

### 1. Перезапустить фронтенд

**Остановите фронтенд** (нажмите Ctrl+C в терминале где запущен `npm run dev`)

**Запустите снова:**
```bash
cd frontend
npm run dev
```

### 2. Проверьте что бэкенд запущен

В другом терминале:
```bash
cd backend
npm run dev
```

Должно быть:
```
🚀 Server running on port 5000
```

### 3. Обновите страницу в браузере

Нажмите **Ctrl+F5** (или Cmd+Shift+R на Mac) для жёсткой перезагрузки.

### 4. Проверьте консоль

Откройте консоль браузера (F12) и проверьте:
- ✅ Запросы должны идти на `http://localhost:5000/api/...`
- ✅ Ошибки CORS должны исчезнуть
- ✅ Регистрация должна работать

## Если всё ещё не работает

1. **Убедитесь что оба сервера запущены:**
   - Backend на `http://localhost:5000`
   - Frontend на `http://localhost:3000`

2. **Проверьте `.env` файл:**
   ```bash
   cd frontend
   type .env
   ```
   Должно быть: `VITE_API_URL=http://localhost:5000/api`

3. **Очистите кэш браузера:**
   - Нажмите Ctrl+Shift+Delete
   - Очистите кэш и cookies
   - Или используйте режим инкогнито

4. **Проверьте что бэкенд отвечает:**
   - Откройте `http://localhost:5000/api/health` в браузере
   - Должен вернуть: `{"status":"ok"}`

## Готово! 🎉

После перезапуска фронтенда всё должно заработать!

