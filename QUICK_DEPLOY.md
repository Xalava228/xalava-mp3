# 🚀 Быстрый деплой на хостинг

## ✅ Проект готов к деплою!

Все необходимые исправления выполнены. Проект готов к размещению на хостинге.

## 📋 Шаги для деплоя

### 1. Установите недостающие зависимости

```bash
cd backend
npm install
```

Это установит `helmet` и другие зависимости.

### 2. Настройте переменные окружения

**Создайте файл `backend/.env`:**

```env
PORT=5000
NODE_ENV=production
FRONTEND_URL=https://yourdomain.com
BASE_URL=https://yourdomain.com
JWT_SECRET=<сгенерируйте безопасный ключ>
```

**Генерация JWT_SECRET:**
```bash
node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
```

**Создайте файл `frontend/.env`:**
```env
VITE_API_URL=https://yourdomain.com/api
```

### 3. Соберите проект

```bash
# Backend
cd backend
npm run build

# Frontend
cd frontend
npm run build
```

### 4. Запустите на сервере

**Backend:**
```bash
cd backend
npm start
```

**Frontend:**
- Загрузите содержимое папки `frontend/dist/` на ваш статический хостинг
- Или используйте nginx для раздачи статики

## 🌐 Варианты хостинга

### Вариант 1: VPS (DigitalOcean, Hetzner, etc.)

1. Установите Node.js на сервере
2. Загрузите проект
3. Настройте `.env` файлы
4. Запустите `npm start` в папке backend
5. Настройте nginx для фронтенда и прокси для API

### Вариант 2: Railway / Render / Fly.io

1. Подключите GitHub репозиторий
2. Настройте переменные окружения в панели
3. Укажите build команду: `cd backend && npm install && npm run build`
4. Укажите start команду: `cd backend && npm start`

### Вариант 3: Отдельный хостинг для фронтенда и бэкенда

- **Backend**: Railway, Render, Fly.io, Heroku
- **Frontend**: Vercel, Netlify, Cloudflare Pages

## ⚠️ Важные моменты

1. **JWT_SECRET** - ОБЯЗАТЕЛЬНО измените на случайную строку!
2. **BASE_URL** - должен указывать на реальный домен вашего сервера
3. **FRONTEND_URL** - должен указывать на домен фронтенда
4. **Резервное копирование** - настройте автоматическое копирование `backend/data/` и `backend/uploads/`

## ✅ Проверка после деплоя

1. Откройте сайт
2. Зарегистрируйтесь
3. Загрузите тестовый трек
4. Проверьте что трек виден и воспроизводится
5. Проверьте что файлы доступны по URL

## 🎉 Готово!

Проект полностью готов к хостингу. Все критические проблемы исправлены.

