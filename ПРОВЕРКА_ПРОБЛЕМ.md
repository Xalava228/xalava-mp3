# 🔍 Проверка проблем с сайтом

## Проблема 1: Главная страница пустая

**Причина:** API запросы не работают - неправильный `VITE_API_URL` или CORS блокирует запросы.

### Решение:

1. **Откройте консоль браузера (F12)**
2. Перейдите на вкладку **Network**
3. Обновите страницу
4. Посмотрите какие запросы идут и какие ошибки

**Если видите ошибки CORS:**
- В Railway → Backend → Variables добавьте:
  ```
  FRONTEND_URL=*
  ```
  (или конкретный URL фронтенда)

**Если видите 404 или неправильный URL:**
- В Railway → Frontend → Variables проверьте:
  ```
  VITE_API_URL=https://your-backend-url.railway.app/api
  ```
- URL должен быть реальным URL вашего бэкенда
- URL должен заканчиваться на `/api`

---

## Проблема 2: Не могу зарегистрироваться

**Причина:** `JWT_SECRET` не установлен в Railway.

### Решение:

1. В Railway откройте сервис **Backend**
2. Перейдите в **Variables**
3. Добавьте переменную:

```
JWT_SECRET=ваш-случайный-секретный-ключ-минимум-32-символа
```

**Как сгенерировать ключ:**
- Можно использовать: https://randomkeygen.com/
- Или в терминале: `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`

**ВАЖНО:** После добавления переменной Railway автоматически перезапустит сервис.

---

## Проблема 3: Ошибка "Произошла ошибка" при регистрации

### Проверьте:

1. **Консоль браузера (F12 → Console)** - там будет точная ошибка
2. **Логи Railway** - Backend → Logs
3. **Переменные окружения:**
   - `JWT_SECRET` установлен?
   - `FRONTEND_URL` установлен?
   - `VITE_API_URL` установлен в Frontend?

---

## Быстрая проверка всех настроек

### Backend Variables (Railway):
```
NODE_ENV=production
JWT_SECRET=<ваш-ключ-минимум-32-символа>
FRONTEND_URL=*
BASE_URL=https://your-backend-url.railway.app
```

### Frontend Variables (Railway):
```
VITE_API_URL=https://your-backend-url.railway.app/api
```

**Замените `your-backend-url.railway.app` на реальный URL!**

---

## Как проверить что всё работает:

1. Откройте сайт
2. Нажмите F12 → Console
3. Должны быть запросы к `/api/tracks`, `/api/podcasts` и т.д.
4. Если видите ошибки - скопируйте их и проверьте переменные

---

## Если ничего не помогает:

1. Проверьте что бэкенд запущен (Backend → Logs должны показывать "Server running")
2. Проверьте что фронтенд собран (Frontend → Logs)
3. Проверьте URL бэкенда - откройте в браузере: `https://your-backend-url.railway.app/api/health`
   - Должен вернуть: `{"status":"ok"}`

