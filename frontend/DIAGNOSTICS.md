# Диагностика белого экрана

## Шаги для решения проблемы:

### 1. Проверьте консоль браузера
- Нажмите F12 в браузере
- Перейдите на вкладку "Console"
- Посмотрите, есть ли красные ошибки
- Скопируйте текст ошибок

### 2. Убедитесь, что фронтенд запущен
```bash
cd frontend
npm run dev
```

Должно появиться сообщение:
```
VITE v5.x.x  ready in xxx ms

➜  Local:   http://localhost:3000/
```

### 3. Проверьте, что бэкенд запущен
```bash
cd backend
npm run dev
```

Должно быть сообщение:
```
🚀 Server running on port 5000
```

### 4. Проверьте зависимости
Если видите ошибки о недостающих модулях:
```bash
cd frontend
npm install
```

### 5. Очистите кэш и переустановите
```bash
cd frontend
rm -rf node_modules
rm package-lock.json
npm install
npm run dev
```

### 6. Проверьте переменные окружения
Убедитесь, что файл `.env` в папке `frontend` содержит (если нужно):
```
VITE_API_URL=http://localhost:5000/api
```

### Частые ошибки:

1. **"Cannot find module"** - нужно запустить `npm install`
2. **"Failed to fetch"** - бэкенд не запущен или неправильный URL
3. **"SyntaxError"** - ошибка в коде, проверьте консоль
4. **"Hydration error"** - проблема с SSR (но у нас его нет)

### Если ничего не помогает:
1. Откройте консоль браузера (F12)
2. Скопируйте все красные ошибки
3. Проверьте вкладку "Network" - есть ли запросы к API
4. Проверьте, что порт 3000 не занят другим приложением



