# 📝 Гайд по обновлению проекта

## 🚀 Как обновить проект на Railway

### Шаг 1: Внесите изменения в код

1. Откройте проект в редакторе
2. Внесите нужные изменения
3. Протестируйте локально (если возможно):
   ```bash
   # Backend
   cd backend
   npm run dev
   
   # Frontend (в другом терминале)
   cd frontend
   npm run dev
   ```

### Шаг 2: Закоммитьте и запушьте изменения

```bash
# Перейдите в корень проекта
cd C:\Users\bogda\OneDrive\Desktop\kyrsach

# Проверьте что изменилось
git status

# Добавьте все изменения
git add .

# Создайте коммит с описанием
git commit -m "Добавлен новый функционал: [опишите что добавили]"

# Отправьте на GitHub
git push
```

### Шаг 3: Railway автоматически обновит проект

1. Railway автоматически обнаружит изменения в GitHub
2. Начнётся процесс сборки (Build)
3. После сборки проект автоматически перезапустится
4. Подождите 2-5 минут

### Шаг 4: Проверьте результат

1. Откройте ваш сайт
2. Проверьте что новый функционал работает
3. Если есть ошибки - проверьте логи в Railway

---

## 🔧 Добавление нового функционала

### Пример: Добавление новой страницы

#### 1. Создайте компонент/страницу

```typescript
// frontend/src/pages/NewPage.tsx
export default function NewPage() {
  return <div>Новая страница</div>
}
```

#### 2. Добавьте маршрут

```typescript
// frontend/src/App.tsx
import NewPage from './pages/NewPage'

// В Routes добавьте:
<Route path="/new-page" element={<NewPage />} />
```

#### 3. Добавьте API endpoint (если нужно)

```typescript
// backend/src/routes/newRoute.ts
router.get('/new-endpoint', async (req, res) => {
  res.json({ message: 'Новый endpoint' })
})
```

#### 4. Зарегистрируйте маршрут

```typescript
// backend/src/index.ts
import newRoutes from './routes/newRoute'
app.use('/api/new', newRoutes)
```

#### 5. Закоммитьте и запушьте

```bash
git add .
git commit -m "Добавлена новая страница и API endpoint"
git push
```

---

## 📦 Добавление новых зависимостей

### Frontend

```bash
cd frontend
npm install название-пакета
git add package.json package-lock.json
git commit -m "Добавлен пакет: название-пакета"
git push
```

### Backend

```bash
cd backend
npm install название-пакета
git add package.json package-lock.json
git commit -m "Добавлен пакет: название-пакета"
git push
```

---

## 🔐 Изменение переменных окружения

### В Railway:

1. Откройте нужный сервис (Backend или Frontend)
2. Перейдите в **Variables**
3. Добавьте/измените переменную
4. Railway автоматически перезапустит сервис

**Важно:** После изменения переменных подождите 1-2 минуты

---

## 🐛 Если что-то пошло не так

### Проверьте логи

1. В Railway откройте сервис
2. Перейдите в **Logs**
3. Найдите ошибки (красным цветом)

### Откатите изменения

```bash
# Посмотрите историю коммитов
git log

# Откатитесь на предыдущий коммит
git revert HEAD

# Или откатитесь на конкретный коммит
git revert <commit-hash>

# Отправьте изменения
git push
```

### Пересоберите проект вручную

1. В Railway откройте сервис
2. Перейдите в **Deployments**
3. Нажмите **"Redeploy"** на последнем деплое

---

## 📋 Чеклист перед обновлением

- [ ] Протестировал изменения локально
- [ ] Проверил что нет ошибок компиляции (`npm run build`)
- [ ] Обновил документацию (если нужно)
- [ ] Создал понятный commit message
- [ ] Запушил изменения на GitHub
- [ ] Проверил что Railway успешно собрал проект
- [ ] Протестировал на продакшене

---

## 💡 Полезные команды

### Локальная разработка

```bash
# Запустить бэкенд
cd backend
npm run dev

# Запустить фронтенд (в другом терминале)
cd frontend
npm run dev

# Собрать проект для проверки
cd backend && npm run build
cd frontend && npm run build
```

### Git команды

```bash
# Посмотреть что изменилось
git status
git diff

# Отменить изменения в файле
git checkout -- путь/к/файлу

# Посмотреть историю
git log --oneline

# Создать новую ветку (для экспериментов)
git checkout -b новая-ветка
```

---

## 🎯 Типичные сценарии

### Добавил новую страницу
1. Создал компонент
2. Добавил маршрут в `App.tsx`
3. `git add . && git commit -m "..." && git push`

### Добавил новый API endpoint
1. Создал роут в `backend/src/routes/`
2. Зарегистрировал в `backend/src/index.ts`
3. `git add . && git commit -m "..." && git push`

### Изменил стили
1. Изменил CSS/Tailwind классы
2. `git add . && git commit -m "..." && git push`

### Добавил новую переменную окружения
1. Использовал в коде: `process.env.NEW_VAR`
2. Добавил в Railway → Variables
3. `git add . && git commit -m "..." && git push`

---

## ⚠️ Важные моменты

1. **Всегда тестируйте локально** перед пушем
2. **Проверяйте логи Railway** если что-то не работает
3. **Не удаляйте переменные окружения** без необходимости
4. **Делайте понятные commit messages** - потом будет легче найти изменения
5. **Не коммитьте `.env` файлы** - используйте Railway Variables

---

## 📞 Если нужна помощь

1. Проверьте логи в Railway
2. Проверьте консоль браузера (F12)
3. Проверьте что все переменные окружения установлены
4. Убедитесь что код компилируется без ошибок

**Готово! Теперь вы знаете как обновлять проект! 🎉**

