# ✅ Чеклист для деплоя

## Перед деплоем

### 1. Установка зависимостей

```bash
# Backend
cd backend
npm install

# Frontend  
cd frontend
npm install
```

### 2. Настройка переменных окружения

**backend/.env:**
```env
PORT=5000
NODE_ENV=production
FRONTEND_URL=https://yourdomain.com
BASE_URL=https://yourdomain.com
JWT_SECRET=<сгенерируйте безопасный ключ>
```

**frontend/.env:**
```env
VITE_API_URL=https://yourdomain.com/api
```

**Генерация JWT_SECRET:**
```bash
node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
```

### 3. Сборка проекта

```bash
# Backend
cd backend
npm run build

# Frontend
cd frontend
npm run build
```

### 4. Проверка структуры директорий

Убедитесь что существуют:
- `backend/data/` - для JSON файлов базы данных
- `backend/uploads/audio/` - для аудио файлов
- `backend/uploads/covers/` - для обложек

### 5. Запуск

```bash
# Backend
cd backend
npm start

# Frontend (статический хостинг)
# Загрузите содержимое frontend/dist/ на ваш хостинг
```

## Проверка работоспособности

### ✅ Тесты:

1. **Регистрация/Вход**
   - [ ] Регистрация нового пользователя работает
   - [ ] Вход работает
   - [ ] Токен сохраняется

2. **Загрузка треков**
   - [ ] Загрузка трека работает
   - [ ] Загруженный трек виден всем пользователям
   - [ ] Аудио файл доступен по URL
   - [ ] Обложка отображается

3. **Воспроизведение**
   - [ ] Треки воспроизводятся
   - [ ] Прогресс сохраняется
   - [ ] Очередь работает

4. **Безопасность**
   - [ ] Rate limiting работает (попробуйте загрузить >10 треков за час)
   - [ ] Неавторизованные пользователи не могут загружать
   - [ ] JWT_SECRET проверяется

5. **Файлы**
   - [ ] Загруженные файлы доступны по `/uploads/audio/...`
   - [ ] Обложки доступны по `/uploads/covers/...`
   - [ ] Все пользователи видят загруженные треки

## Важные моменты

### Хранение данных

- **JSON файлы**: `backend/data/` - настройте резервное копирование
- **Загруженные файлы**: `backend/uploads/` - настройте резервное копирование
- **Рекомендация**: Используйте облачное хранилище для файлов (S3, Spaces)

### Мониторинг

- Настройте логирование ошибок
- Мониторинг дискового пространства
- Мониторинг производительности

### Безопасность

- ✅ JWT_SECRET настроен
- ✅ Helmet включен
- ✅ Rate limiting активен
- ✅ CORS настроен правильно
- ✅ Валидация файлов работает

## Готово! 🚀

Проект готов к продакшену!

